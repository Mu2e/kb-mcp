# Dependencies needed to import kb_mcp and run tests/unit.
#
# Deliberately excludes sentence-transformers (and therefore torch and the
# NVIDIA CUDA wheels): the unit tests never load an embedding model, while a
# full `pip install .[test]` resolves ~135 packages and several GB of GPU
# libraries - far too slow for a per-push CI job.
#
# Used by .github/workflows/tests.yml. Install the package itself with
# --no-deps alongside this file. If a unit test starts needing something new,
# add it here.

pytest>=8.0.0
pytest-asyncio>=0.23.0

sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0
python-dotenv>=1.0.0

tiktoken>=0.5.0
beautifulsoup4>=4.12.0
python-pptx>=0.6.0
pillow>=10.0.0
numpy>=1.24
tqdm>=4.65
anyio>=4.0
